# Phase 2 User Flow Contract

Every flow below is a contract boundary. Screen IDs are from the screen catalogs; API names are from `04_API_ENDPOINT_CATALOG.md`. A flow may be implemented only after its API/database contract is accepted.

| Flow ID / actor | Preconditions | Screen → action | API | Business rule | DB entities | Result / next state |
|---|---|---|---|---|---|---|
| `FLOW-AUTH-001` User login | Account exists; service available. | `SYS-W-001` enter credentials → submit. | `POST /auth/login`, optional MFA. | Rate limit; verify credential; MFA if configured; never reveal account existence. | CP users/credentials/sessions/mfa. | Authenticated session → App Shell or MFA. |
| `FLOW-PLAT-001` Gym provisioning | Authenticated Platform Admin with `platform.provision` and verified MFA step-up; new org/Gym input and valid registered server target. | Existing registry entry → `PA-W-004` monitor. | `POST /api/v1/platform/provisioning`, status, retry. | `Requested` → `Provisioning` → `Migrating` → `Seeding` → `Verifying` → `Active`; four named failure states; no partial Active. | CP organizations/Gyms/servers/DB registry/provisioning/IAM/audit. | Active Gym or safe retryable/non-retryable failure. |
| `FLOW-MEM-001` Create member | Gym context; actor has `members.create`. | `MEM-W-002` fill exact fields → save. | `POST /gyms/{gymId}/members`. | Backend validates TOP GYM field rules; no duplicate policy invented beyond contract. | `members.members`, audit. | Member created → profile or list. |
| `FLOW-MEM-002` Edit member | Member belongs to current Gym; Draft/current version. | `MEM-W-002` edit → save. | `PUT /gyms/{gymId}/members/{memberId}`. | Scope + rowversion; archive only through delete permission. | members.members/audit. | Updated profile; stale conflict if version changed. |
| `FLOW-MEM-003` Member profile | Read permission for member and selected tabs. | `MEM-W-003` open → tabs. | member detail/timeline/membership/attendance/measurements/plan/doc endpoints. | Per-tab sensitive filtering; no cross-Gym query. | member domain tables. | Profile view; next action governed by tab permission. |
| `FLOW-MEM-004` Membership lifecycle | Member exists; applicable membership permission. | `MEM-W-004` add/freeze/renew/payment. | memberships/freeze/renew/payments. | Dates/freeze/payment validation; events append-only; financial fields protected. | memberships/events/payments. | Active/frozen/renewed state + timeline/audit. |
| `FLOW-MEM-005` Attendance | Member exists; check-in/out permission. | `MEM-W-003` attendance tab → check-in/out. | attendance endpoint. | No duplicate open attendance; UTC timestamp; backend authority. | attendance/audit. | Attendance record; dashboard/report consumers. |
| `FLOW-MEM-006` Measurements | Member exists; exact measurement fields. | `MEM-W-005` add measurement → save. | measurements endpoint. | Only audited fields; units/range validation; sensitive access. | body_measurements/audit. | Measurement history and valid Nutrition context. |
| `FLOW-CRM-001` CRM lead | Gym actor has CRM permission. | `CRM-W-001` create/update/activity/follow-up. | CRM lead/activity/follow-up endpoints. | Manual WhatsApp prepare/open only; no API send. | CRM tables/audit. | Lead pipeline/timeline; conversion possible. |
| `FLOW-CRM-002` Convert lead | Lead eligible; `crm.convert` + `members.create`. | `CRM-W-001` convert → member form/confirm. | convert endpoint. | Transactional conversion; no duplicate conversion. | lead_conversions + members.members. | Lead converted and member created; timeline link. |
| `FLOW-FIN-001` Payment | Member/membership or sale reference; finance permission. | `MEM-W-004` or `FIN-W-001` enter payment. | payments endpoint. | Amount/method/reference; EGP default/configurable currency; DECIMAL money; operational finance/audit. | payments/membership or sale/audit. | Payment transaction/receipt source. |
| `FLOW-FIN-002` Expense/closing | Finance actor; business date. | `FIN-W-001` enter expense/declare closing. | expenses/daily-closings. | Explicit currency/tax/method; server daily close and cash variance; audit. | expenses/closings/payments/refunds. | Open/closed/variance state. |
| `FLOW-STORE-001` Store sale | Active SKU/stock; POS actor; credit sales disabled by default. | `STORE-W-001` select/cart/member or guest → pay. | products/sales. | Server calculates totals/stock/tax; Weighted Average Cost; idempotent transactional sale. | products/sale lines/movements/payments. | Sale completed or safe failure; receipt. |
| `FLOW-INV-001` Inventory count/adjust | Inventory permission; location configured. | `INV-W-001` start count → submit variance. | inventory counts/movements. | Append-only ledger; no direct balance overwrite; variance movement is permissioned/audited and transactional. | counts/lines/movements. | Count pending/closed and audited movement. |
| `FLOW-LIB-001` Exercise library | Canonical seed installed; library read. | `LIB-W-001` search/detail/custom edit. | exercises/metadata/custom CRUD. | Canonical IDs active; custom ownership; no numeric TOPGYM PK reuse. | library exercise/lookups/media refs. | Library result used by Training. |
| `FLOW-LIB-002` Food library | Canonical seed installed; library read. | `LIB-W-002` search/detail/custom edit. | foods/metadata/custom CRUD. | Source/version/unit/nutrition required; no unsupported conversion. | library food/lookups. | Food result used by Nutrition. |
| `FLOW-TRN-001` Training creation | Member/context valid; creator permission. | `TRN-W-002` choose Manual/Automatic/AI/Hybrid → build → Save Draft. | create/generate/update. | All modes produce same canonical model; AI only canonical Exercise IDs; no publish. | training plans/days/exercises + audit. | Draft. |
| `FLOW-TRN-002` Training review | Draft passes structural/domain validation; submit permission. | `TRN-W-002` submit → `TRN-W-003` review. | submit-review/review. | Draft→Review; reviewer may return changes; audit. | plan/reviews/audit. | Review or Draft returned. |
| `FLOW-TRN-003` Training approval/publish | Review complete; approver/publisher permissions. | `TRN-W-003` approve → publish; `TRN-W-004` view. | approve/publish/version. | Creator cannot approve own plan; Published snapshot immutable. | reviews/versions/plan/audit. | Approved→Published→Version/Snapshot. |
| `FLOW-TRN-004` Workout session | Published version exists; member/trainer session permission. | `TRN-W-004` start/log/finish. | sessions/set logs. | Log references exact published version; no plan mutation. | sessions/set_logs. | Session completed/progress data. |
| `FLOW-NUT-001` Nutrition creation | Member context and inputs valid; creator permission. | `NUT-W-002` choose mode → calculate → meals/foods → Draft. | create/calculate/generate/update. | SC-015 engine; canonical Food IDs; 1–12 meals; no publish. | plan/targets/calculations/meals/foods. | Draft. |
| `FLOW-NUT-002` Nutrition review | Draft totals/targets validated; submit permission. | `NUT-W-002` submit → `NUT-W-003`. | submit-review/review. | Review can return changes; calculations rerun through engine. | reviews/calculations/plan. | Review or Draft returned. |
| `FLOW-NUT-003` Nutrition approval/publish | Review complete; distinct approver if required; publisher permission. | `NUT-W-003` approve → publish; `NUT-W-004` view. | approve/publish/version. | Creator cannot approve own plan; formula/food values snapshot; no recalc history. | reviews/versions/calculations/meals. | Approved→Published→immutable snapshot. |
| `FLOW-NUT-004` Meal log | Member has published version. | `NUT-W-004` choose meal/quantity → log. | nutrition logs. | Log references version and snapshots actual values; no plan edit. | meal_logs/food_logs. | Logged meal/totals. |
| `FLOW-CLS-001` Class/booking | Session exists within one-time/weekly boundaries. | `CLS-W-001` view → book/cancel/waitlist. | classes/sessions/bookings. | Configurable capacity; reject full unless waitlist enabled; FIFO; two-hour default cancellation cutoff; state always backend. | definitions/sessions/bookings/waitlist/attendance. | Confirmed/waitlisted/cancelled/no-show. |
| `FLOW-PORTAL-001` Member Portal | Portal session or approved member auth; member scope. | `POR-W-001/002` navigate. | portal endpoints. | Only safe member data and Published plans; no admin/other Gym data. | member/membership/attendance/versions/docs. | Portal view/log/download. |
| `FLOW-QR-001` Public QR | Token presented; no login. | `POR-W-003` open `/qr/:token`. | `GET /qr/{token}`. | Hash lookup; opaque token; minimal allowlist; no Member ID/sensitive fields. | qr_tokens + Gym context/branding. | Generic valid/invalid response. |
| `FLOW-PRINT-001` Print | Authorized source entity/version. | `PRT-W-001` open preview → print. | print endpoint. | Render from authoritative API/snapshot; Arabic/RTL; no mutation. | source entity/version + branding. | Browser print-ready document. |
| `FLOW-PDF-001` PDF | Authorized source entity/version; local renderer available. | `PRT-W-001` download PDF. | pdf endpoint. | Local adapter; no paid provider/CDN requirement; errors safe. | source entity/version + report run if persisted. | Local PDF stream/ref or safe failure. |

## Final gap-resolution flow addendum — 2026-08-25

| Flow ID | Actor / precondition | Screen / action | API | Business rule | DB / audit | Result / next state |
|---|---|---|---|---|---|---|
| `FLOW-AUTH-002` TOTP MFA | Authenticated identity enrolling or challenge pending. | `SYS-W-001` enroll/verify/disable/recover. | MFA endpoints. | Authenticator App/TOTP; Email OTP is not primary; secret protected; recovery codes one-time. | IAM MFA/recovery/session tables; redacted audit. | Verified MFA or safe failure/rate limit. |
| `FLOW-AUTH-003` Password reset | User can request reset without account enumeration. | `SYS-W-001` request/complete. | Password reset endpoints. | Hash-only, single-use, expiring token; secure password hash; invalidate sessions as required. | IAM reset/session/audit. | Password changed or generic failure. |
| `FLOW-PORTAL-AUTH-001` Member-code portal access | Member has valid code; Gym is resolved from protected code. | `POR-W-000`/`F-POR-000` enter code. | `POST /member-portal/access`, portal logout. | Rate-limited, member/Gym scoped, revocable, audited; no traditional member login; no numeric public ID. | Portal access codes/sessions; audit. | Portal session → safe portal home. |
| `FLOW-FIN-003` Refund | Original payment/sale exists; refund permission and reason. | `FIN-W-001` refund dialog. | Refund endpoint. | Full/partial refund; link original; server currency/tax/balance; audit. | Refund/payment/sale/audit. | Refunded/partially refunded. |
| `FLOW-STORE-002` Sale/return | Product/stock and configured payment method; credit disabled. | `STORE-W-001` cart → pay or return. | Sale/return endpoints. | Weighted Average Cost, explicit tax default 0%, transactional inventory/finance, no hard delete. | Sales/lines/movements/refunds/audit. | Completed/Voided/Refunded/Partially Refunded. |
| `FLOW-CLS-002` Capacity/waitlist/attendance | Session within recurrence boundaries. | `CLS-W-001` book/cancel/check in/no-show. | Booking/cancel/attendance endpoints. | Capacity rejection unless waitlist enabled; FIFO waitlist; two-hour default cutoff; no-show separate. | Session/bookings/waitlist/attendance/audit. | Confirmed/waitlisted/cancelled/attended/no-show. |
| `FLOW-CRM-002` Follow-up | Lead exists and owner permission. | `CRM-W-001` schedule/complete/filter overdue. | Follow-up/activity endpoints. | Due date/type/note/owner/completed; overdue visible; manual WhatsApp only. | CRM follow-ups/activities/timeline/audit. | Open/completed/overdue view. |

## Phase 5B Authentication/RBAC flow addendum — 2026-08-26

| Flow ID | Actor / precondition | Screen | API | Business rule | DB / audit | Result |
|---|---|---|---|---|---|---|
| `FLOW-AUTH-004` Password change | Authenticated active user with `auth.password.change`. | `SYS-W-001`/`SYS-W-002`; `F-AUTH-001`. | `POST /auth/password/change`. | Verify current password; validate new password; revoke all active sessions on success. | `iam.credentials`, `iam.sessions`, redacted audit. | Reauthentication required. |
| `FLOW-AUTH-005` MFA recovery | MFA challenge and valid recovery code. | `SYS-W-001`; `F-AUTH-001`. | Existing `POST /auth/mfa/verify` with `method=recovery_code`. | One-time hash match; no silent TOTP/recovery fallback; rate-limited. | `iam.mfa_recovery_codes`, sessions, audit. | MFA challenge completed or safe failure. |
| `FLOW-AUTH-006` Session security | Authenticated user with session permissions. | `SYS-W-002`; `F-AUTH-001`. | `GET /auth/sessions`; `POST /auth/sessions/{id}/revoke`. | Own current/authorized scope only; raw tokens never returned. | `iam.sessions`, audit for revoke. | Safe sessions listed/revoked. |
| `FLOW-AUTH-007` Access administration | Security administrator with `platform.security.manage`. | `PA-W-007`. | `/platform/access/*` addendum endpoints. | Control Plane security operation; explicit Gym target authorization; no implicit Gym business access. | IAM users/roles/permissions/assignments, audit. | User/role scope changed or safely denied. |

## Phase 7 provisioning flow closure - 2026-08-29

`FLOW-PLAT-001` is finalized by the Phase 7 contract package. A verified
Platform Admin submits a new organization/Gym request with an idempotency
key; the server creates the Control Plane registry records and returns a
202 response containing the operation ID. A worker performs placement,
database creation, EF migration, canonical seed, verification, Owner
initialization, and activation in the approved order. The Web client polls
the existing status route and may request a controlled retry only for a
retryable failure. No client supplies a Gym ID, physical database name,
connection string, or credential. Platform/Gym boundaries and the exact state
machine are defined in `../PHASE_7/05_PROVISIONING_LIFECYCLE.md`.
