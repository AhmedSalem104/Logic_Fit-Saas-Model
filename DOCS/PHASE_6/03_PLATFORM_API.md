# Phase 6 Platform API Assessment (Superseded)

**Status:** SUPERSEDED by `03_PLATFORM_API_CONTRACT.md`

This file preserves the initial Phase 2 route inventory. The canonical Phase
6 API is the exact read-only route list in `03_PLATFORM_API_CONTRACT.md`.
Routes without a complete approved schema are explicitly outside the Phase 6
slice; no compatibility route is introduced.
